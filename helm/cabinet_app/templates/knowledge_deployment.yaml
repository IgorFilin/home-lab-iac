apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.knowledge.deployment_name }}
spec:
  replicas: {{ .Values.knowledge.replicas }}
  selector:
    matchLabels:
      {{ toYamlPretty .Values.front.label }}
  
  template:
    metadata:
      labels:
        {{ toYamlPretty .Values.front.label }}
    spec:
      containers:
        - name: knowledge
          {{- with .Values.knowledge.image }}
          image: "{{ .repository }}:{{ .version }}"
          {{- end }}
          imagePullPolicy: {{ .Values.knowledge.imagePullPolicy }}
          env:
            {{- range .Values.knowledge.envs }}
              {{- $data := dict "name" .Values.backend_secret.name "env" .  }}
              {{-  include "env.template" $data | indent 12 }}
            {{- end }}
          resources:
            limits:
              {{ .Values.knowledge.limits }}
            requests:
              {{ .Values.knowledge.requests }}
      imagePullSecrets:
        - name: {{ .Values.docker_login_secret.name }}
